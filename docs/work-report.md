# Отчёт о работе

Журнал сессий в этом каталоге. Новые записи — сверху. Цифры только из фактических прогонов. Не путать с [status.md](status.md) (текущее состояние) и [decisions.md](decisions.md) (принятые выборы).

---

## 2026-09-18 — обзор для решения

**Запрос.** Один большой md: что достигнуто, архитектура, куда
дальше. Код не писать.

**Сделано.** [overview-2026-09-18.md](overview-2026-09-18.md).
Цифры только из уже записанных F*. Развилки А–Е. Новых прогонов нет.

---

## 2026-09-18 — сетка 2560×1600

**Запрос.** Разрешение экрана 2560×1600.

**Сделано.** `ref_window` = 2560×1600. Слот Б x=2140. Оверлей
масштабирует на окно хоста. Тесты слотов зелёные. ADR-0077.

**Не сделано.** Посадка окон оператором. Новый `layout-slots`.

---

## 2026-09-18 — живой оверлей рамок

**Запрос.** Рамки поверх окна игры, чтобы оператор сам
перетаскивал окна L2.

**Сделано.** `layout-overlay`, Swift click-through слой.
Слоты-цели возвращены (А 60/140, Б w-420/140). Тесты слотов
зелёные. Слой запущен на 16372, HID нет. ADR-0076.

**Не сделано.** Посадка окон оператором. Повтор `layout-slots`
после посадки.

---

## 2026-09-18 — Ход 86, сетка слотов

**Запрос.** Жёсткие слоты A/B, оверлей, `WindowManager`, живой
Tab/Escape, без фарма.

**Сделано.** Код, тесты слотов. Оверлей `run/layout_guide.png`.
Живой SUCCESS 1.330 с: слот Б свободен → Tab занял → Escape
освободил. SHA1 `1ac0f4eb…`. ADR-0075. Граф 3400 узлов.

**Не сделано.** Посадка скилов/макросов. Q5. Слот А как диалог
на живом HTML в этом ходе не меряли.

---

## 2026-09-18 — Ход 85, F2 и обход меню

**Запрос.** Фундаментальный контур: `/target`, F2-подбег, строгая
левая панель, проход всех пунктов меню.

**Сделано.** Код, полный `pytest`. Живой crawl: 6 ссылок слева,
6 кликов, маркер цели не сработал, 36.67 с, SHA1 `ce2cec6a…`.
ADR-0074. Граф 3328 узлов.

**Не сделано.** OCR пунктов. Q5. Стабильный SCK без рестартов
(7 `no frame` за прогон).

---

## 2026-09-18 — Ход 84, чат-таргет

**Запрос.** `/target` вместо F5, починить SCK на ЛКМ, доказать
открытие диалога из закрытого окна (прогон h).

**Сделано.** Код и полный `pytest`. ADR-0073. Граф обновлён
(3309 узлов). Живой h не стартовал: окно 16372 `on_screen=false`.

**Не сделано.** F84, JSON h, замер SCK после PID-only клика,
факт открытия окна кликом по модели.

---

## 2026-09-18 — Ход 83, диалог NPC

**Запрос.** F5, подход, детекция HTML-окна, клик по ссылке.

**Сделано.** Код и тесты. Два живых прогона: лок после F5 есть,
окно не детектировано, клика нет. F2 не слали. ADR-0071.
SHA1 `d016a14a…`, повтор `684b00cc…`.

**Не сделано.** Подтверждённое HTML-окно и клик. Чтение квестовых
веток.

---

## 2026-09-18 — Ход 82, серия 10

**Запрос.** 10 фрагов без таймера, сгладить агро.

**Сделано.** Живой **150.0 с**, **10/10**. L1 0, агро 2, разворот 2,
средний бой 3.35 с, stuck_keys 0. ADR-0070. SHA1 `388921ec…`.

**Не сделано.** Q5 / профиль камеры. Навигацию это не закрывает.

---

## 2026-09-18 — Ход 81, агро и жёсткий L1

**Запрос.** Приоритет бьющего моба, убрать ложный L1, серия
5 фрагов без таймера, без банок.

**Сделано.** Живой прогон **94.6 с**, **5/5**. L1 0, разворот 1,
агро 0, stuck_keys 0. ADR-0069 (не 0066). SHA1 `63a421ac…`.
`tactical.py` / Frozen L1 не трогали.

**Не сделано.** Живой перехват в спину в этом прогоне не
сработал: `aggro_interrupts=0`.

---

## 2026-09-18 — Ход 80, интегрированный спот

**Запрос.** L1 поток + L2 FSM, 10 фрагов, не Frozen L1.

**Сделано.** Живой прогон 251.9 с, **7/10**, abort `self_hp_low`.
roam 24, L1 60, stuck_keys 0. SHA1 `ed36f5eb…`. F80 обновлён.

**Не сделано.** 10/10. Серия упёрлась в HP без банок.

---

## 2026-09-18 — сводка H17

**Запрос.** Полный отчёт по H17, без новых прогонов.

**Сделано.** [h17-report.md](evidence/h17-report.md): формулировка,
три смысла «stuck», F61–F72 и F79, ADR-0061…0064, развилка.
Нового HID нет. Гипотеза открыта.

**Не сделано.** Принять или отклонить H17.

---

## 2026-09-18 — Ход 74, стрелка klt_physical

**Запрос.** Продолжать калибровку, пока оператор не вернётся.
Оператор смотрел yaw и шаг.

**Сделано.** 89 live JSON. Два `arrow_pulse`: 146 и 113 инлаеров,
ratio ≥0.70, `|dx|≈40`. Код `q5_closed=true`. Профиль не писали.
Оператор: нигде не застревал. 7 коротких flow: `stuck_detected=false`.
F79. Новых HID после возврата нет.

**Не сделано.** `|dx|≥50` при том же consensus. Запись профиля.
Правка выбора RMB vs стрелка при одном `pixel_diff`.

---

## 2026-09-18 — Ход 73, батарея калибровки

**Запрос.** Продолжать тесты, пока оператор не вернётся.

**Сделано.** 26 live JSON. `SEARCH=80`, `--mouse-dy`. Мёртвая зона
мыши, согласие coarse/KLT на 54 px, повтор −270/−288 на крыше.
walk 66 px / 500 мс. Q5 нет. F78. Тесты klt+cgevent зелёные.
`graphify update .`: 3104 узла, 8331 ребро.

**Не сделано.** Закрытие Q5. Запись профиля. 30 инлаеров KLT.

---

## 2026-09-18 — Ход 72, сильнее yaw

**Запрос.** Крути сильнее.

**Сделано.** Копия кадра в `grab()`, settle 0.30 с, сессия 20 с.
RMB +800 / 40×20 мс. coarse_dx 1313, diff 14.33, physical=true.
Стрелку не слали. Q5 нет. F77. `graphify update .`: 3091 узел,
8312 рёбер. Тесты `test_klt_yaw` 11 зелёных.

**Не сделано.** Закрытие Q5. Запись профиля.

---

## 2026-09-18 — Ход 71, coarse live без копии кадра

**Запрос.** Погнали.

**Сделано.** Живой `--rmb` +480. HID ушёл. T0≡T1 (общий буфер SCK),
coarse_peak 0.966, diff 0.15. F76.

**Не сделано.** Закрытие Q5. Достоверный замер yaw.

---

## 2026-09-18 — Ход 70, KLT не видит большой yaw

**Запрос.** Оператор видел разворот ≈45° на hard yaw.

**Сделано.** F74 «камера не поехала» снято. H18 принята. `coarse_shift`
(фаза + pixel_diff, маска персонажа) и PNG `yaw-*-t0/t1.png`.
`physical` = KLT или грубый сдвиг; `q5_closed` только с KLT `|dx|`.
Тесты 11 в `test_klt_yaw.py`. `graphify update .`: 3084 узла, 8292
ребра. Живой повтор не слали.

**Не сделано.** Закрытие Q5. Запись профиля. Живой JSON с coarse_dx.

---

## 2026-09-18 — Ход 69, сильный yaw

**Запрос.** Крути сильнее.

**Сделано.** RMB 480 px / 0.6 с, стрелка 24 тапа + 800 мс. dx ≈ 0.
Q5 нет. F74. CLI `--mouse-dx` / `--arrow-hold-ms`. Тесты 24 зелёных.

**Не сделано.** Закрытие Q5. Запись профиля.

---

## 2026-09-18 — Ход 68, yaw у стены

**Запрос.** Смотрю на стену — короткий yaw.

**Сделано.** `--rmb`: KLT 39/39, dx 0; стрелка dx ≈ 0. Q5 нет. F73.
Профиль не писали. Frozen L1 не трогали.

**Не сделано.** Закрытие Q5. Запись motion в профиль.

---

## 2026-09-18 — Ход 67, основной live-motion

**Запрос.** Основные тесты — не бой.

**Сделано.** 10+3 × 600 мс, 8.39 с. пик 0.136, duplicate 0/13.
stuck false. F72. Frozen L1 не трогали.

**Не сделано.** `flow_free ≥ 0.25`. Фарм. Бой.

---

## 2026-09-18 — Ход 66, 8 с

**Запрос.** Сессия ок, прогнать 8 с.

**Сделано.** 10+3 × 600 мс, 8.66 с. flow 0.014, пик 0.095,
с free[7] duplicate. F71. Frozen L1 не трогали.

**Не сделано.** Подтверждение шага оператора после прогона. H17.

---

## 2026-09-18 — Ход 65, второй короткий `w`

**Запрос.** Ещё раз.

**Сделано.** 3×500 мс, 2.19 с. duplicate нет, flow 0. F70.
8 с не слали. Frozen L1 не трогали.

**Не сделано.** Длинный прогон. H17.

---

## 2026-09-18 — Ход 64, короткий `w` после релога

**Запрос.** Запустить тест.

**Сделано.** 3×500 мс, 1.93 с. Кадр живой, flow 0.034, F69.
Первый захват — SourceLost, повтор на видимом окне. Длинный
прогон не слали (ADR-0064). Frozen L1 не трогали.

**Не сделано.** 8 с. Закрытие H17.

---

## 2026-09-18 — Ход 63, повторный срыв

**Запрос.** Странно, что обрыв второй раз после теста.

**Сделано.** F68, H17, ADR-0064. Двойной `w` up. Длинный прогон
отложен. Frozen L1 / `sim/` не трогали.

**Не сделано.** Доказать причину. Снять сокет.

---

## 2026-09-18 — Ход 62, nudge после «обрыва»

**Запрос.** Попробовать подвигать персонажа самим HID.

**Сделано.** 1.92 с непрерывный `w`, hid=true, stuck_keys=0.
3/3 duplicate, flow 0. F67. Frozen L1 не трогали.

**Не сделано.** Доказать обрыв сети. Фарм.

---

## 2026-09-18 — Ход 61, рестарт окна 16372

**Запрос.** Готов к прогонам после рестарта клиента.

**Сделано.** Новый `capture list`: окно 16372. Непрерывный `w`.
magnitude 0.044/0, пик 0.231, `approach` на free[1].
`stuck_detected=false`. F66. Frozen L1 / `sim/` не трогали.

**Не сделано.** `flow_free ≥ 0.25`. Фарм.

---

## 2026-09-17 — Ход 60, линейка закрыта

**Запрос.** Завершить линейку, чтобы перезапустить клиент.

**Сделано.** Стоп HID. Сводка F61–F65. ADR-0063. Окно 13944
списано. Frozen L1 / `sim/` не трогали. Фарм не открывали.

**Не сделано.** Закрытие Q5. `stuck_detected`. Живой прогон после
рестарта.

---

## 2026-09-17 — Ход 59, непрерывный `w` в клиенте

**Запрос.** Прогнать непрерывную ходьбу.

**Сделано.** 2 HoldKey, stuck_keys=0, magnitude 0.0048/0, пик 0.048.
С free[6] duplicate. encode p95 48.4 мс. `stuck_detected=false`. F65.
Frozen L1 / `sim/` не трогали.

**Не сделано.** `flow_free ≥ 0.25`. Фарм.

---

## 2026-09-17 — Ход 58, непрерывный `w`

**Запрос.** Персонаж ходит шажками, так в игре не ходят.

**Сделано.** `live-motion`: один `w` down, сэмплы, один up.
ADR-0062. Тесты 4 зелёных. Frozen L1 / `sim/` не трогали.

**Не сделано.** Живой прогон непрерывной ходьбы. Фарм.

---

## 2026-09-17 — Ход 57, подход к стене

**Запрос.** Повторить `live-motion`: подход, затем упор.

**Сделано.** Новый JSON. magnitude 0.0139/0, пик 0.095.
free[5..9]+blocked — duplicate, confidence 0. encode p95 47.7 мс.
`stuck_detected=false`. F64. Frozen L1 / `sim/` не трогали.

**Не сделано.** `flow_free ≥ 0.25`. Фарм.

---

## 2026-09-17 — Ход 56, поток у стены

**Запрос.** Повторить `live-motion` после упора в стену.

**Сделано.** Тот же протокол, новый JSON. magnitude 0.0137/0,
confidence ≈ 0.45/0.16, encode p95 48.4 мс. blocked[1..2]
duplicate + confidence 0. `stuck_detected=false`. F63.
Frozen L1 / `sim/` не трогали. Код не меняли.

**Не сделано.** `flow_free ≥ 0.25`. Фарм.

---

## 2026-09-17 — Ход 55, поток у ворот деревни

**Запрос.** Оператор встал перед воротами; повторить `live-motion`.

**Сделано.** Тот же протокол ADR-0061, новый JSON. magnitude 0.0089/0,
confidence ≈ 0.51/0.53, encode p95 50.1 мс, `stuck_detected=false`.
F62. Frozen L1 / `sim/` не трогали. Код не меняли.

**Не сделано.** `flow_free ≥ 0.25`. Контраст ≥ 3 при ненулевом free.
Фарм.

---

## 2026-09-17 — Ход 54, живой поток на `w`

**Запрос.** 10 тиков свободного бега и упор, калибровка stuck L1.

**Сделано.** `live-motion`: magnitude/expansion 0, confidence ≈ 0.40
на обеих фазах, encode p95 48 мс, `stuck_detected=false`. ADR-0061.
F61. Frozen L1 / `sim/` не трогали. Тесты 4 зелёных.

**Не сделано.** Контраст ≥ 3. Аппаратный collision signal. Фарм.

---

## 2026-09-17 — Ход 53, изоляция T0/T1

**Запрос.** Исключить stale/общий буфер, сдампить PNG, получить
ненулевой KLT.

**Сделано.** `--sync-diag`: копии, 300 мс, unique ts, RMB hold до T1.
`diff_mean` 7.05, Δt 371 мс, KLT dx ≈ 0. SCK desync нет. ADR-0060.
F60. Тесты 31 зелёных на затронутых файлах.

**Не сделано.** Закрытие Q5. Запись motion в профиль. Автономный агент.

---

## 2026-09-17 — Ход 52, RMB Drag / pulse

**Запрос.** Доказать физический yaw (RMB Drag или arrow pulse) и
закрыть Q5.

**Сделано.** `rmb_drag` + `arrow_pulse` + `--rmb`. Живой прогон:
KLT 55 точек / 39 инлаеров, median_dx ≈ 0. Профиль не писали.
ADR-0059. F59. Тесты 34 зелёных на затронутых файлах.

**Не сделано.** Закрытие Q5. Запись `motion` в профиль. 360°
ориентир. Автономный агент.

---

## 2026-09-17 — Ход 51, KLT yaw

**Запрос.** Закрыть Q5 через KLT/RANSAC, right+left 450 мс.

**Сделано.** `estimate_yaw_klt` + `--klt`. Живой прогон: inliers
22/25, dx ≈ 0, профиль не писали. ADR-0058. F58. Тесты 16 зелёных
на затронутых файлах.

**Не сделано.** Закрытие Q5. Запись `camera_deg_per_sec`. 360°
ориентир. Автономный агент.

---

## 2026-09-17 — Ход 50, H8 и горизонт Q5

**Запрос.** Честный p95 H8 на 100 тиках и yaw по полосе горизонта.

**Сделано.** `bench-h8`: p95 H8 2.19 мс, pipeline 16.33 мс,
hid_sent=false. Горизонт: peak 0.013, профиль не писали.
ADR-0057. F57. `pytest` 26 зелёных на затронутых тестах.

**Не сделано.** Закрытие Q5. Запись motion в профиль. Автономный агент.

---

## 2026-09-17 — Ход 49, калибровка кадра

**Запрос.** Замер yaw/шага, закрыть Q5 и H8.

**Сделано.** `calibrate-motion`: 20.8 px / 165 px, peak низкий,
stuck=0. Блок `motion` в профиле с `confidence=low`. ADR-0056. F56.

**Не сделано.** Закрытие H8. Закрытие Q5. Автономный агент.

---

## 2026-09-17 — Ход 48, стрелка камеры

**Запрос.** Камера `right_arrow`, не мышь. Один килл.

**Сделано.** 0x7C 450 мс, килл 0.444→0.0, F3, stuck=0. Мыши нет.
ADR-0055. F55. `pytest` 30 зелёных.

**Не сделано.** Пустой радиус + шаг `w`. Замер yaw глазами. Q5. H8.

---

## 2026-09-17 — Ход 47, поиск

**Запрос.** `--verify-search`, пустой радиус, один шаг, один килл.

**Сделано.** Ветка маневра сработала дважды: rotate+`w`, stuck=0.
Лока нет, килла нет, второго шага нет. ADR-0054. F54.
`pytest` 20 зелёных.

**Не сделано.** Захват после поиска. Q5 (метры/yaw). H8.

---

## 2026-09-17 — Ход 46, три цикла

**Запрос.** `--multi-kill 3`, 45 с, без бесконечного фарма.

**Сделано.** SCAN/COMBAT/LOOT/RESET. 3 фрага, 3 F3, 27.33 с,
stuck=0. ADR-0053. F53. `pytest` 17 зелёных.

**Не сделано.** Факт лута в инвентаре. Фарм без потолка. H8.

---

## 2026-09-17 — Ход 45, килл и F3

**Запрос.** Один моб до смерти, F3, без фарма.

**Сделано.** `--kill-and-loot`. 0.444→0.0 за 6685 мс, F3 sent,
stuck=0. ADR-0052. F52. `pytest` зелёный.

**Не сделано.** Факт лута в инвентаре. Фарм. H8.

---

## 2026-09-17 — Ход 44, урон

**Запрос.** Снять `clean_hp_low`, бить цель с HP≥0.15, двойной F2.

**Сделано.** `--engage-any`. 0.444 → 0.366 за 1031 мс. stuck=0.
ADR-0051. F51. `pytest` зелёный.

**Не сделано.** Килл. Новый моб. H8.

---

## 2026-09-17 — Ход 43, clean-target

**Запрос.** Сброс Escape, новый моб HP>0.85, двойной F2, окно 8 с.

**Сделано.** `--clean-target`. Сброс прошёл. F1×3 → 0.444. F2 нет.
Урона нет. ADR-0050. F50. `pytest` зелёный.

**Не сделано.** Чистый захват. Дельта HP. H8.

---

## 2026-09-17 — Ход 42, S4-зонд

**Запрос.** Открыть S4. Один цикл ≤15 с: F1 → lock → F2 → HP → Escape.

**Сделано.** `l2-brain s4-probe`. Окно 13944. 5.43 с, 76 тиков,
hid_sent=true, lock 0.444 за 116 мс, дельты HP нет, Escape, stuck=0.
ADR-0049. F49. `pytest` зелёный.

**Не сделано.** Урон. Подход. H8. H14. Фарм.

---

## 2026-09-17 — Q6, допуск Interlude

**Запрос.** Оператор уже на сервере; смотреть историю прошлого чата.

**Сделано.** Транскрипт `dc2b2375` пуст (лимит модели). Q6 закрыт по
кадрам F44/F45 и ответу оператора: `LineageII Interlude Server`,
BotEbaniy, говорящий остров / Gremlin. ADR-0048. F48.

**Не сделано.** S4. HID в `L2.exe`. Проверка, что это локальный L2J.

---

## 2026-09-17 — Ход 41, TargetLost и хотбар

**Запрос.** Не считать сброс живой цели победой. Слоты F1–F3.
Без HID и без S4.

**Сделано.** `EntityDefeated` только при HP=0 на рамке или пропаже
при HP≤0.08. Drop 0.44 → `TargetState(locked=false)`. ROI F1–F3 с
кадра F45, все ready. ADR-0047. F47. `graphify update .`: 2547 узлов.
`pytest` зелёный.

**Не сделано.** S4. Живая спираль кулдауна. Поиск сдвинутого окна.

---

## 2026-09-17 — Ход 40, смерть ≠ сброс цели

**Запрос.** Рамка и полоска HP раздельно. F44 строго без цели.
`EntityDefeated` при смерти. Без HID и без S4.

**Сделано.** `detect_target_plate` по подложке Classic. F45 locked
hp 0.444; F44 locked=false hp=None. Пустая рамка → dead. Шина
`EntityDefeated` один раз. ADR-0046. F46. `graphify update .`: 2527
узлов. `pytest` зелёный.

**Не сделано.** S4. Поиск сдвинутого окна. Хотбар. Отличие Escape
от смерти при пропаже рамки.

---

## 2026-09-17 — Ход 39, измеренные ROI HUD

**Запрос.** Продолжить живой край после обрыва чата. Полосы ещё не
были размечены.

**Сделано.** ROI с кадра Gremlin в `parallels_l2.json`. Парсер:
self 0.943/0.952/0.962, target_locked, target_hp 0.444. Замок цели
по красной полосе. `--from-png`. ADR-0045. F45. HID нет.
`graphify update .`: 2501 узел. `pytest` зелёный.

**Не сделано.** S4. H8. H14. Хотбар. Q6.

---

## 2026-09-17 — Ход 38, фокус / калибровка HUD

**Запрос.** Убрать osascript с проверки фокуса, снять кадр клиента и
оверлей ROI. Боевой ввод запрещён.

**Сделано.** NSWorkspace ctypes. focus p50 0.0047 мс; контроль osascript
175.9 мс. act_ms RecordingPoster 0.016 мс, hid_sent=false. SCK кадра
13944: оконный Interlude, плейсхолдеры мимо полос. ADR-0044. F44.
`graphify update .`: 2478 узлов. Тесты 19 passed.

**Не сделано.** S4. H8. Измеренные `self_hp` / `target_hp`. Повтор live HID.

---

## 2026-09-17 — Ход 37, Блокнот live

**Запрос.** Повторить probe без `L2.exe`, hid_sent, stuck=0, watchdog.

**Сделано.** Консольный Notepad запущен. Live: hid_sent=true, stuck=0,
watchdog отпустил `w`, p50/p95 215.6/244.1 мс. Кадр: `123` в Notepad.
`graphify update .`: 2433 узла. Тесты 9 + 6.

**Не сделано.** S4. H8. Чистый Untitled без уже открытого `l2.log`.

---

## 2026-09-17 — Ход 36, CGEvent / S3

**Запрос.** Живой актуатор CoreGraphics, watchdog, probe в Блокнот.
Не слать в клиент игры. `sim/` и Frozen не трогать.

**Сделано.** `CGEventInputBackend` с двумя флагами. Фокус / watchdog /
F12-state. `input-probe`. Тесты 8 + старые 6 = 14 passed. Live abort:
`l2.exe` в госте, `hid_sent=false`. Dry-сценарий: watchdog stuck=0.
`graphify update .`: 2424 узла.

**Не сделано.** Подтверждение клавиш в Notepad. S4. H8. Вариант B.

---

## 2026-09-17 — Ход 35, парсер HUD

**Запрос.** Этап 2: `HUDParser`, мост в `TelemetryHub`, ROI в профиле,
синтетические тесты, ADR-0042. Без HID, pcap и правок `sim/`.

**Сделано.** Левый цветовой профиль полосы. События `ui_vision`.
`pytest tests/test_hud_parser.py` — 9 passed. ROI — типичный Classic,
не замер клиента. GT стенда не отключали.

**Не сделано.** Живая разметка кадра. OCR. Ввод. Подключение к политике.

---

## 2026-09-17 — Ход 34, H7 на окне Parallels

**Запрос.** Этап 1: список окон, профиль, 30 с H7, вердикт ADR-0041.
Не трогать sim/, control/, Frozen 60. Без HID и guest-софта.

**Сделано.** Окно `Windows 11` / Parallels Desktop, id 13745,
2056×1290 точек, захват 4112×2580. Профиль `parallels_l2.json`.
30.13 с: медиана 33.76 мс, 29.62 FPS, drops 0, black 0,
latency p50/p95 10.97/12.73 мс. H7 принята. `graphify update .`:
2274 узла, 6048 рёбер. pytest `test_h7_probe` + `test_sck`: 13 passed.

**Не сделано.** HUD. Живой HID. Кадр в политике. H8. Этап 2–4.

---

## 2026-09-17 — План перехода на Parallels / Lineage 2

**Запрос.** Зафиксировать 4 этапа живого края: SCK окна VM, HUD,
ввод, короткий спот. Клиент не подключать в этом шаге.

**Сделано.** [live-parallels-plan.md](live-parallels-plan.md). Q1/Q2
уточнены. Следующий код — этап 1 (H7 на видимом окне Parallels).

**Не сделано.** Захват VM. Живой HID. Парсер UI. S4.

---

## 2026-09-17 — Ход 33, непрерывный спот

**Запрос.** Цикл SCAN→…→LOOT на 3 манекенах, сторожа, dry-run, без клиента.

**Сделано.** `sim/spot_arena.py`, `control/spot_agent.py`, события лута,
CLI `spot-loop`. ADR-0040, F39. Baseline не меняли.

**Прогоны.** 4 теста зелёные. 250 тиков: 4/4 фраги и лут, серия 4.
[spot-loop.json](evidence/hod33/spot-loop.json).

**Не сделано.** Живое окно. HID. Frozen 60.

---

## 2026-09-17 — Ход 32, итоговая мини-сводка

**Запрос.** 5 контроллеров × 5 сцен, без правок политик и без Frozen 60.

**Сделано.** `bench/suite_runner.py`, CLI `suite`, чекпоинт `w_in` хода 27.
ADR-0039, F38.

**Прогоны.** baseline 5/5, snn/rstdp/malecns 4/5, GRU 3/5.
[final-comparison.json](evidence/hod32/final-comparison.json).

**Не сделано.** Frozen 60. H3/H4. Живая стыковка. Обучение GRU.

---

## 2026-09-17 — Ход 31, синтетический подграф MaleCNS

**Запрос.** Блок 6: компактный visuo-motor срез, контроли bio/shuffle/ER,
сравнение с `snn_v1` на `open_goal:training:v0:s0`. Полный коннектом
запрещён. Рабочие контроллеры не трогать.

**Сделано.** `control/malecns/` (loader, LIF-сеть, контроллер, compare),
CLI `malecns-eval`, 8 тестов. ADR-0038, F37. FlyEM не скачивался.

**Прогоны.** snn 61 / bio 178 / shuffle 191 / ER timeout 200.
[malecns-comparison.json](evidence/hod31/malecns-comparison.json).

**Не сделано.** H11. Полный MaleCNS. Frozen 60. Замена `snn_v1`.

---

## 2026-09-17 — Ход 30, профиль контура

**Запрос.** Замерить стадии тика и проверить MPS vs CPU. Не оптимизировать.

**Сделано.** `bench/profiler.py`, `bench/mps_audit.py`, CLI `profile`.
ADR-0037, F36. Политики не меняли.

**Прогоны.** 100 тиков × 3 контроллера. SNN infer p95=1.70 мс. MPS B=1
медленнее. [profiling-m4.json](evidence/hod30/profiling-m4.json).

**Не сделано.** Перенос на MPS. Metal. Правка encode.

---

## 2026-09-17 — Ход 29, телеметрия и бой на стенде

**Запрос.** Шина тактических событий и синтетический `combat_dummy_v0`.
Без клиента, pcap и HID. Без правок baseline.

**Сделано.** `telemetry/`, `sim/combat.py`, `control/tactical.py`.
ADR-0036, F35.

**Прогоны.** 5 тестов зелёные. Success 80 тиков, 100 урона, полная FSM.
[combat-dummy-v0.json](evidence/hod29/combat-dummy-v0.json).

**Не сделано.** Живая телеметрия UI. Бой в клиенте. Frozen 60.

---

## 2026-09-17 — Ход 28, InputBackend dry-run

**Запрос.** Игровой адаптер ввода: действия, профиль, декодер, watchdog.
Строго dry-run. Без инъекций и без HID.

**Сделано.** `src/l2_brain/io/`, CLI `input-dry-run`. ADR-0035, F34.
`contracts.py` и симулятор не меняли. S2 не открывали.

**Прогоны.** 6 тестов зелёные. Лог 6 событий, `hid_sent=false`
([dry-run.json](evidence/hod28/dry-run.json)).

**Не сделано.** Живой ввод. Калибровка окна. S2/S3. Привязка к клиенту L2.

---

## 2026-09-17 — Ход 27, локальное обучение

**Запрос.** RewardEngine, R-STDP, тесты, короткая серия на `open_goal`.
Не клиент, не Frozen 60, не baseline.

**Сделано.** `learning/reward.py`, `control/snn/plasticity.py`,
`l2-brain learn`, ES-заготовка для GRU. ADR-0034, F33.

**Прогоны.** 10 тестов пластичности зелёные. Контроль 59, eval 58,
train 59/59/59/60/58/58/59/58.
[open-goal-rstdp.json](evidence/hod27/open-goal-rstdp.json).

**Не сделано.** Frozen 60. H3/H4. Обучающая серия GRU. Заявление, что
сеть «научилась ходить».

---

## 2026-09-17 — Ход 26, GRU и единый мини-бенч

**Запрос.** Компактный GRU на тех же 6 признаках и раннер четырёх
контроллеров. Не Frozen 60. Без тяжёлого обучения.

**Сделано.** `control/gru/`, `control/bench.py`, `l2-brain bench`.
ADR-0033, F32. Baseline и `control/snn` (кроме `n_params()`) без смены
динамики.

**Прогоны.** 5 тестов GRU + 9 SNN зелёные.
`open_goal:training:v0:s0`: baseline 72, GRU 162, SNN 61, все success.
JSON: [hod26/open-goal-training-v0.json](evidence/hod26/open-goal-training-v0.json).

**Не сделано.** Frozen 60. H3/H4. Обучение GRU. Заявление, что SNN лучше.

---

## 2026-09-17 — Ход 25, рефлекс Брайтенберга

**Запрос.** Откалибровать вход и readout SNN. `open_goal:training:v0` ≤120
тиков. Без STDP, без Frozen 60, infer < 2 мс.

**Сделано.** Детерминированные пулы и нормированный readout. ADR-0032, F31.

**Прогоны.** 9 тестов зелёные. v0: **success 61**, col=0, infer **1.704** мс.

**Не сделано.** Frozen 60. H3/H4. Пластичность.

---

## 2026-09-17 — Ход 24, SNN sandbox + Graphify

**Запрос.** Изолированный LIF (`control/snn`), тесты физиологии, один
`open_goal:training:v0`. Обновить правила под Graphify.

**Сделано.** Пакет SNN, хук `run_episode(controller=)`, CLI `--controller snn`.
Правила ядра/graphify/10/20/30/55. `graphify update .`. ADR-0031, F30.

**Прогоны.** `test_snn_dynamics` 6 passed.
`open_goal:training:v0` timeout, infer **1.653** мс, rate 52.2 Гц
([open-goal-training-v0.json](evidence/hod24/open-goal-training-v0.json)).

**Не сделано.** Frozen 60. H3/H4. Успех open_goal. Обучение.

---

## 2026-09-17 — Milestone: заморозка L1

**Запрос.** Зафиксировать 52/60 как конец калибровки эвристики. Не крутить
константы. Подтвердить готовность каркаса к изолированному `control/snn`.

**Сделано.** ADR-0030, правило `55-l1-baseline-freeze.mdc`, осмотр протокола.
Пакет SNN не создавали. Baseline и `frozen-hod23.json` не меняли. F29.

**Прогоны.** Новых нет. Канон по-прежнему [frozen-hod23.json](evidence/hod23/frozen-hod23.json) **52/60**.

**Не сделано.** Реализация LIF. H3/H4. S2. L2/L3.

---

## 2026-09-17 — Ход 23, изоляция курса от обхода

**Запрос.** Вернуть 6 регрессий post-h22: vanish 5/5, `narrow_gate` train v1,
`open_goal` held_out v4. Забор и wrap-константы не трогать. Без имён сцен, порог 0.12.

**Сделано.** `live` vs память курса. Hold после `had_target` без Probe/Peel, пока
нет expansion ≥0.08. Живой lock — capped track 0.16. ADR-0029, F28.

**Прогоны.** vanish 5/5 (62–65). gate v1 **84**. open v4 **73**. corridor v4 **95**.
box v0 185, weak 191, dead_end 87. fence v0 timeout 200 col=0.
Frozen [frozen-hod23.json](evidence/hod23/frozen-hod23.json) **52/60**. Δ к post-h22: +6/−0.

**Не сделано.** Финиш забора в 200. S2. `moving_target` held_out v4 и box v2/v3.

---

## 2026-09-17 — Кинематика забора и Frozen 60

**Запрос.** Не менять политику. Проверить `long_fence` на 250 тиках. Снять Frozen 60.
Сравнить с post-h14 44/60. Разложить провалы.

**Сделано.** v0 на 250: success **244**, col=0
([long-fence-250.json](evidence/post-h22/long-fence-250.json)). Срез
[frozen-post-h22.json](evidence/post-h22/frozen-post-h22.json) **46/60**. ADR-0028, F27.

**Прогоны.** Забор 250: v0 244, v1 250, v3 timeout dist=1.30, v2/v4 stuck col=77/76.
Frozen 200: 46 success, 7 timeout, 7 stuck, oscillate 0. Δ к post-h14: +8/−6.
Ожидание 50–54/60 и финиш v0 в 220–230 не подтвердились.

**Не сделано.** Правка vanish/пеленга. Подъём канона до 250. S2.

---

## 2026-09-17 — Budget trim

**Запрос.** Уложить `long_fence` в 200: разгон Probe, короче Slide 2, спринт после wrap.
Не ломать `dead_end` ≤150 и бокс/weak ≤195.

**Сделано.** Probe 0.75 отвергнут (бокс stuck). reslide 32, wrap-clear 8, спринт 1.0.
ADR-0027, F26.

**Прогоны.** `dead_end` 147. `single_obstacle` **193**. `weak_texture` **190**. Пеленговые
без регрессии. `long_fence` timeout 200, col=0, **LOS 182**, **acq 183**, dist=4.47.
pytest зелёный.

**Не сделано.** Финиш на заборе в 200. Frozen 60. S2.

---

## 2026-09-17 — Post-Edge Wrap

**Запрос.** После прохода торца не продолжать южный Slide: доворот за угол, захват,
финиш `long_fence` ≤200. Не ломать `single_obstacle` / `weak_texture` / `dead_end`.

**Сделано.** Capped wrap → вынос → seek. После wrap `hook_clear` не держит курс.
ADR-0026, F25.

**Прогоны.** `dead_end` 147. `single_obstacle` **193**. `weak_texture` **190**. Пеленговые
без регрессии. `long_fence` timeout 200, col=0, **LOS 185**, **acq 186**, конец (7.76, 4.32),
dist=5.12. pytest зелёный.

**Не сделано.** Финиш на заборе в 200 тиках. Frozen 60. S2.

---

## 2026-09-17 — Edge-loss Hook

**Запрос.** Срывать Slide по окончанию кромки, не по 22 тикам. Не ломать `dead_end` ≤150,
`single_obstacle`/`weak_texture` ≤195 и пеленговые. Проверить торец и LOS на `long_fence`.

**Сделано.** min/max Slide; abort hook при фронте после 26 тиков → align + 40 Slide. ADR-0025, F24.

**Прогоны.** `dead_end` 147. `single_obstacle` **193**. `weak_texture` **190**. Пеленговые без
регрессии. `long_fence` stuck 200, edge да, `min_y=3.11` (торец пройден), **LOS нет**.
pytest зелёный.

**Не сделано.** Финиш/LOS на заборе. Frozen 60. S2.

---

## 2026-09-17 — Corner Hook

**Запрос.** Ограничить Slide бюджетом и завернуть за угол (`-sign`). Не ломать `dead_end` ≤150
и пеленговые сцены. Проверить LOS и финиш на `single_obstacle`.

**Сделано.** Slide 22 → hook `-sign`. После пятна: выравнивание стороны, 16 тиков курса,
пол seek 0.68. ADR-0024, F23.

**Прогоны.** `dead_end` 147. `single_obstacle` **success 190**, LOS 118, hook 95, min_x=3.45,
col=0. `weak_texture` 191. Пеленговые без регрессии. `long_fence` timeout, LOS нет.
pytest зелёный.

**Не сделано.** Обход длинного забора. Frozen 60. S2.

---

## 2026-09-17 — Probe Gate

**Запрос.** Разделить реакцию на препятствие по дистанции свободного хода. Не ломать `dead_end` ≤150,
`u_trap`, `open_goal`, `narrow_gate`, `corridor`, `latency_drops`. Проверить LOS на `single_obstacle`.

**Сделано.** Probe `0.45/0` → latch по `probe_distance_ticks` (`<14` CLOSE_TRAP Peel `turn=1.0` на месте;
иначе Peel 26 + Slide). ADR-0023, F22.

**Прогоны.** `dead_end` 147 (probe=9, seen=23). `u_trap` 52. Пеленговые без регрессии (72 / 84 / 94 / 76).
`single_obstacle` stuck 200, **LOS нет**, probe=46 deep, min_x=0.59. Забор/weak: stuck, seen=None.
pytest зелёный.

**Не сделано.** Финиш у стены. Frozen 60. S2.

---

## 2026-09-17 — Front-clear Peel/Slide

**Запрос.** Развязать Peel и Slide по освобождению фронта, не одним таймером со спавна. Не ломать `dead_end` ≤150, `open_goal`, `narrow_gate`.

**Сделано.** `front_blocked` = steer/expansion. `initial_front_blocked` после warmup. Нет фронта — Peel на месте 40 тиков; контакт после подхода — Peel ~120° и Slide `turn=0`. ADR-0022, F21.

**Прогоны.** `dead_end` 134, `u_trap` 64, `open_goal` 72, `narrow_gate` 84, `corridor` 94, `latency_drops` 76. Стена/забор/weak: stuck 200, LOS нет. `single_obstacle` span_x=2.92 min_x=3.40 col=0; `long_fence` span_x=2.99 min_x=0.41; `weak_texture` span_x=2.84 min_x=3.40. pytest зелёный.

**Не сделано.** Финиш у стены. Frozen 60. S2.

---

## 2026-09-17 — Peel & Slide

**Запрос.** Две фазы слепого хода: отворот, затем скольжение вдоль кромки. Не ломать `dead_end`.

**Сделано.** `_blind_command`: Peel / Slide; сброс лока при пятне; `had_target` чтобы не вползать в стену после срыва. `peel_ticks=40` по замеру 0.081 рад/тик (8 тиков ≠ 180°).

**Прогоны.** `dead_end` 140, `open_goal` 72, `narrow_gate` 84. Стена/забор/weak: stuck/timeout 200, цель не в кадре, Slide уносит назад. pytest зелёный.

**Не сделано.** Проход вдоль барьера до LOS. S2.

---

## 2026-09-17, ход 15 — Frozen 60 и side-bias

**Запрос.** Сначала канон 60 без правок. Затем обход без пеленга: гистерезис стороны и запрет осцилляции yaw. Без S2, карт, веток по сцене, без перезаписи F14/F15.

**Сделано.** Срез [post-h14/frozen-baseline.json](evidence/post-h14/frozen-baseline.json). `side` + `side_lock` 20 тиков; вслепую `forward>0.2`. ADR-0021, F20. Отчёт: [hod15-report.md](evidence/hod15-report.md).

**Прогоны.** Freeze 44/60 (osc 11, stuck 3, timeout 2). После правки 15/15 стеновых: oscillate→stuck, dir=0, финиша нет. `open_goal` 72, `narrow_gate` 84, `dead_end` 141. pytest зелёный.

**Не сделано.** Проход вокруг препятствия до цели. S2, SNN, полный 60 после шага 2.

---

## 2026-09-17, ход 14 — четыре бага контура

**Запрос.** По очереди закрыть C4/C7, S4, S6, S3/V1. Без S2, SNN, карт, без снижения 0.12 и без перезаписи F14/F15.

**Сделано.** Синхронизирован `cmd_forward_min`. Course lock + запрет evidence на транзите. Изоляция потока на drop (`stale_flow_hold=2`), пеленг на stale не крутится командой. Гистерезис трека 0.12/0.08 только для трека или компактного центра. ADR-0020, F19. Отчёт: [hod14-report.md](evidence/hod14-report.md).

**Прогоны.** pytest зелёный. CLI 8 сцен: 39/40. `narrow_gate` 200 timeout → 84 success (5/5). `latency_drops` 0/5 → 5/5. `corridor` 200 timeout → 94 success (5/5), FP пустых 0/4. `open_goal` 170→72, `dead_end` 155→138, `u_trap` 195→179 — все success. F14/F15 SHA на месте.

**Не сделано.** S2, SNN, полный frozen 60. `single_obstacle` / `long_fence` / `weak_texture` по-прежнему oscillate. `moving_target` held_out v4 stuck — не тюнили.

---

## 2026-09-17, ход 13 — H16, не S2

**Запрос.** Не идти на следующий этап. Добавить кратковременную память и восстановление; оценить тупики и П-ловушки.

**Сделано.** Состояния и явная логика уже были (`ShortTermMemory`, `score_recovery`). Попытка зажечь recovery у стены ломала `dead_end` — откатили. Отчёт: [h16/report.md](evidence/h16/report.md). Карты нет. Не биология.

**Прогоны.** training v0: после «злого» детектора dead_end 1→0; после отката u_trap/dead_end снова успех без recovery. Frozen H16 не улучшен.

**Не сделано.** S2, SNN. Критерий «статистически лучше на тупиках» не выполнен: на каталоге нечего улучшать, а на реальной стене recovery не помогает без регрессии.

---

## 2026-09-17, ход 12 — ремонт метрик, H15, SAD

**Запрос.** Посмотреть правки оценщика; сделать то, без чего нельзя сдвинуть этап.

**Сделано.** Дописан [repair-20260917/report.md](evidence/repair-20260917/report.md). Абляции политики `--channels` / `--encoder`. План и прогон H15. Векторизация SAD (22 → 1.7 мс), эталон цикла сохранён в тесте. ADR-0019, F16–F18.

**Прогоны.** H15 frozen seed=0: blob 22, target 22, flow 21, expansion 20, all 24. Δ +0.033, гипотеза отклонена. pytest зелёный. F14/F15 не трогали.

**Не сделано.** S2, H1 на 20 эп., SNN, MaleCNS, Metal. Контур к recurrent не готов.

---

## 2026-09-16, ход 11 — целостность экспериментов

**Запрос.** Приостановить SNN/MaleCNS/обучение. Устранить разрыв постановки, наблюдений и критериев. 13 блоков, по очереди, без перезаписи исходных JSON.

**Сделано.** Freeze + воспроизведение 23/60. Одна приёмка nav-sim. События ≠ один stuck. Наблюдаемость, suite integrity, `push_wall`, бюджет в секундах, A/B/C, фабрика энкодера, маски цели, правки курса/ворот. Итог: [integrity/13-final.md](evidence/integrity/13-final.md).

**Прогоны.** Repro frozen: 23/60, 0 расхождений с F14/F15. Скрипт стены: physical_blockage. A/B/C на 5 сценах v0. Маски старта. Локально после курса/ворот: open_goal 170, gate 0 столкновений / timeout, vanish всё ещё не доходит. Полный каталог после правок политики не гоняли.

**Не сделано.** H15 абляция, новый held-out, SNN, MaleCNS, Metal. Контур не готов к recurrent.

---

## 2026-09-16, ход 10 — кратковременная память и восстановление

**Запрос.** Состояния рабочей памяти без координатной карты; застревание не по одному потоку; явное восстановление; отдельные метрики детекции/FP/времени/успеха/retrap. Критерий: лучше на тупиках и U-ловушках, без существенного ухудшения простых сцен.

**Сделано.** `ShortTermMemory` + явная логика восстановления в `BaselineController` (`baseline_memory_v1`). Ситуации stand / camera_turn / goal_like / unreliable / blocked / move. Повтор сцены только при структуре ближнего плана. ADR-0017, F15, H16, `docs/evidence/baseline-memory.md`. Не биологический механизм.

**Прогоны.** `pytest` 93 passed. Каталог seed=0 vs F14: 23/60 те же исходы. Ловушки 10/10, ticks_p50 117.5 = база. Простые 13/15 = база. GT-окон застревания 0, восстановлений 0, FP 0. Улучшение ловушек не подтверждено, регрессии нет. Held-out не тюнили.

**Не сделано.** Рекуррентная память, H15, H7, живой клиент.

---

## 2026-09-16, ход 9 — прозрачный базовый контроллер

**Запрос.** Контроллер без обучения, чтобы проверить достаточность сенсоров до SNN. Полный набор сцен, диагностика вкладов, классификация провалов, без маршрутов под карты.

**Сделано.** `l2_brain.control.BaselineController`, CLI `baseline-eval`, ADR-0016, `docs/evidence/baseline-controller.md`.

**Прогоны.** `pytest` 84 passed. Каталог seed=0, 60 эпизодов, 23 успеха; stuck 21, timeout 16, oscillate 0; vision 20, control 17. Held-out 4/12, не тюнили по нему.

**Не сделано.** H15, SNN, живой клиент.

---

## 2026-09-16, ход 8 — VisualEncoder для сенсомоторной навигации

**Запрос.** Интерпретируемый энкодер без крупной модели объектов: каналы яркости/контраста/Δ/потока/расширения/пеленга, несколько масштабов, эталонный поток с уверенностью и масками, разделение движения камеры/тела/сцены, диагностика, набор преобразований. Критерий: отличить подход к препятствию от поворота камеры.

**Сделано.** Пакет `src/l2_brain/vision/`, `NavigationEncoder`, CLI `vision bench|diag`, ADR-0015, `docs/evidence/vision-encoder.md`. Color-blob в mock не снимали. Контур политики не гоняли.

**Прогоны.** `pytest` 73 passed. `vision bench --repeats 8`: 48×32 accuracy 6/6, `sim_separates=true`, encode p50 36.57 мс; 32×24 быстрее, на стенде не разделяет. `vision diag` → `run/vision-diag.ppm`, метка `camera_turn`.

**Не сделано.** Сравнение политики (H15), живое окно, H7, детектор объектов.

---

## 2026-09-16, ход 7 — ScreenCaptureKit FrameSource

**Запрос.** Нативный источник кадров macOS, реальные кадры в общий FrameSource, запись и replay. Без авто-разрешений и без shared memory до замера копий.

**Сделано.** Поток в `macos/CaptureProbe` (`SCStream`, RGB pipe `L2F1`). Python `SCKFrameSource`, профили/маски, CLI `capture permission|list|record`. ADR-0014.

**Прогоны.** `pytest` 60 passed. Permission granted без запроса. `capture record --self-test`: кадр 440×344, replay. `--resize`: 2 resize, 20.16 FPS при запросе 20, scale 2. H7 не оценивали.

**Не сделано.** Игровое окно, HID, 30 с H7, shared memory.

---

## 2026-09-16, ход 6 — инфраструктура экспериментов

**Запрос.** Воспроизводимая запись сессий, раздельные часы и стадии задержки, bounded-очереди, replay realtime/speed/step, просмотр проблемного тика.

**Сделано.** Пакет `src/l2_brain/experiment/`, async JSONL + кадры npy, latest-only очередь, CLI `record`/`replay --mode`/`inspect`, ADR-0013.

**Прогоны.** `pytest` 50 passed. `record --ticks 6 --out run/exp-demo` и `inspect --tick 2`: кадр, признаки, intent, command, timings. Offline replay с `closed_loop: false`.

**Не сделано.** S1, живой клиент, GPU compute на MPS.

---

## 2026-09-16, ход 5 — навигационный стенд

**Запрос.** Локальная среда сенсомоторной навигации: 12 сцен, MotorIntent, GT отдельно, сплиты, машинный отчёт. Без клиента.

**Сделано.** Пакет `src/l2_brain/sim/`, CLI `sim-eval`, тесты `tests/test_sim_env.py`, ADR-0012, `docs/evidence/sim-validation.md`.

**Прогоны.** `pytest` 38 passed. `sim-eval --split training --max-steps 2`: 36 эпизодов idle, 0 успехов (ожидаемо), JSON с seed/config.

**Не сделано.** S1 с настоящей политикой, живой клиент, утверждение о переносе.

---

## 2026-09-16, ход 4 — исполняемый каркас контура

**Запрос.** Минимальный каркас: сменные интерфейсы, типизированные структуры, жизненный цикл, CLI mock/record/replay/eval/doctor. Критерий: mock-кадры проходят контур, порождают намерения, пишутся и контур закрывается без клиента.

**Сделано.**

- Контракты: `src/l2_brain/contracts.py` (`Frame`, `TelemetryEvent`, `Observation`, `MotorIntent`, `Command`, `Effect`).
- Контур: `src/l2_brain/circuit/` (mock источник, пустая телеметрия, color-blob encoder, feature controller, pulse decoder, mock backend, JSONL recorder, evaluator, `Circuit`).
- CLI: `python -m l2_brain mock|record|replay|eval|doctor`.
- ADR-0011. Стенд S0 не ломался.

**Прогоны.**

- `pytest`: 28 passed.
- `l2-brain mock --ticks 16 --seed 0 --record run/mock-session.jsonl`: ticks=16, intents=16, dropped=0, mock=true.
- `replay` и `doctor` отработали. Клиент не открывался.

**Не сделано.** S1, живой захват/ввод, обязательные сцены валидации стенда, Metal, MaleCNS.

---

## 2026-09-16, ход 3 — отдельный отчёт

**Запрос.** Вести отчёт о проделанной работе отдельным файлом и включить предыдущее.

**Сделано.** Создан этот файл. В [status.md](status.md) добавлена ссылка на него. Код агента не менялся.

**Не сделано.** Повторный прогон тестов и `evaluate` в этом ходе.

---

## 2026-09-16, ход 2 — правила и skills

**Запрос.** Подготовить правила и project skills. Агента не реализовывать. Старый проект считать удалённым. Существующие пользовательские файлы не перезаписывать.

**Контекст на входе.** Каталог уже не пуст: документы v1 и пакет `l2_brain` лежали с хода 1.

**Создано.**

Правила (только `00-project-core.mdc` с `alwaysApply: true`):

- `.cursor/rules/00-project-core.mdc`
- `.cursor/rules/10-architecture.mdc`
- `.cursor/rules/20-experiments.mdc`
- `.cursor/rules/30-python.mdc`
- `.cursor/rules/40-macos-metal.mdc`
- `.cursor/rules/50-perception-control.mdc`
- `.cursor/rules/60-learning.mdc`

Skills:

- `.cursor/skills/experiment-design/SKILL.md`
- `.cursor/skills/simulator-validation/SKILL.md`
- `.cursor/skills/visual-sensor-engineering/SKILL.md`
- `.cursor/skills/snn-engineering/SKILL.md`
- `.cursor/skills/apple-silicon-profiling/SKILL.md`
- `.cursor/skills/game-adapter-integration/SKILL.md`
- `.cursor/skills/failure-analysis/SKILL.md`

Документы:

- `docs/project-brief.md`
- `docs/open-questions.md`
- `docs/status.md`
- `docs/agent-workflows.md`

**Не трогалось.** `docs/vision.md`, `architecture.md`, `assumptions.md`, `roadmap.md`, `decisions.md`; код в `src/`, `tests/`, `macos/`.

**Проверки этого хода (структура, не загрузка Cursor):**

- расширения `.mdc` и YAML frontmatter;
- у каждого skill шесть разделов процедуры;
- 28 относительных ссылок из новых файлов существуют;
- в новых файлах нет выдуманных API ввода/виртуализации;
- объём: правила 21–37 строк, skills 47–65 строк.

**Не проверялось.** Загрузил ли Cursor правила; поведение агента; H1–H14; интервал кадров ScreenCaptureKit.

**Итог этапа.** Инженерная основа инструкций есть. Формальная реализация агента (S1+, адаптер, обучение) не начиналась.

---

## 2026-09-16, ход 1 — документы v1 и каркас стенда

**Запрос.** Создать `docs/vision.md`, `architecture.md`, `assumptions.md`, `roadmap.md`, `decisions.md` и минимальную структуру, согласованную с репозиторием. Не останавливаться на плане.

**Контекст на входе.** `/Users/apple/Desktop/L2_brain` был пуст. Операционных инструкций агента не было (человек сказал, что напишет их сам). Git — на всём `Desktop`, не на этом каталоге.

**Обнаружено на машине (не из удалённого дерева проекта):**

- Apple M4 Max, arm64;
- Python 3.14 без numpy в системном интерпретаторе;
- Swift 6.3;
- MaleCNS — коннектом дрозофилы, не игровая политика.

**Создано.**

Документы:

- `docs/vision.md` — цель и границы v1;
- `docs/architecture.md` — контур, контракты `Observation` / `Action`, три контроллера;
- `docs/assumptions.md` — факты F1–F9 и гипотезы H1–H14 с проверкой и отклонением;
- `docs/roadmap.md` — этапы S0–S4;
- `docs/decisions.md` — ADR-0001…0010;
- `README.md`, `pyproject.toml`, `.gitignore`;
- `docs/evidence/.gitkeep`;
- `memory/2026-09-16.md`.

Код стенда (`src/l2_brain/`):

- типы, признаки, петля, оценка, метрики;
- контроллеры `reactive`, `recurrent`, `snn`;
- синтетическая среда и сценарии `open_field`, `pursuit`, `occluded`, `memory_probe`;
- заглушки живого клиента, ScreenCaptureKit-в-петле и HID (`NotImplementedError`);
- зонд `macos/CaptureProbe/main.swift` вне петли.

Тесты: `tests/test_synthetic_loop.py`, `test_controllers.py`, `test_live_blocked.py`, `test_metrics.py`.

**Принятые решения (кратко).** Стенд раньше клиента. MaleCNS не зависимость. Три контроллера, один протокол. `infer_ms` ≠ полная задержка. Metal/MPS не включены. Сенсор v1 — кадр. Карт маршрутов нет. Запрещены обход античита, маскировка ввода, инъекции, патч, перехват учёток. `AGENTS.md` не генерировать.

**Прогоны этого хода:**

- `pytest`: 17 passed.
- Дым `evaluate --all-controllers --scenario open_field --episodes 8 --seed 0`: все три контроллера `success=1.00`; `infer_ms` ≈ 0.04 мс, `loop_ms` ≈ 0.93–0.96 мс. Это не приёмка H1 (нужны 20 эпизодов и запись в evidence).
- Дым `memory_probe` ×8: `recurrent` success=1.00; `reactive` и `snn` success=0.00.
- `.build/capture-probe`: список окон (1 дисплей, 21 окно). Интервалы H7 не снимались.

**Не делалось.** Живой клиент, ввод в окно, обучение, коннектом, официальный S1, git-коммит этого каталога.

---

## Сводка на сейчас

| Область | Состояние |
|---|---|
| Канон v1 | есть; ADR до 0015 |
| Правила и skills | есть; постоянно только `00-project-core` |
| Локальный стенд | `sim` 12 сцен; S1 не закрыт |
| VisualEncoder | `NavigationEncoder` 48×32 |
| Базовый контроллер | 23/60 на каталоге; H15 не закрыта |
| Захват | `SCKFrameSource` pipe; H7 не закрыта |
| Игровой адаптер | нет |
| MaleCNS / обучение | нет |
| Следующий шаг | S1 по `experiment-design` или 30 с H7 |

Незакрытые неизвестные — [open-questions.md](open-questions.md). Как вызывать skills — [agent-workflows.md](agent-workflows.md).
