# Ход 63: повторный срыв после `live-motion`

Дата: 2026-09-18. Нового прогона нет. `L2.exe` уже снят.

## Совпадение

| когда | прогон | сразу после |
|---|---|---|
| 2026-09-17 вечер | F65, непрерывный `w` 8.34 с | клиент «завис» |
| 2026-09-18 утро | F66, непрерывный `w` 8.32 с | клик есть, шага нет; F67 кадр мёртвый |

Пульс F61–F64 17-го (тот же вечер, до непрерывного режима) без
такого отчёта.

Зонд не открывает сокет Interlude. HID — `CGEventPostToPid` на
Parallels. Не доказано: flood пакетов хода, клин в стене, alt-tab,
сам сервер.

H17 открыта. ADR-0064: длинный прогон не слать, пока оператор
не походит сам после входа.

F68.
