# Ход 82: серия 10 фрагов, сглаженный агро

Дата: 2026-09-18. Frozen L1 и `sim/` не трогали. Фарм не открывали.
Живой контур — `live/spot_loop.py`. ADR-0070.

## Что сделано в коде

| слой | поведение |
|---|---|
| Агро | кольцо 5 тиков; `mean(prev) − current ≥ 0.015` и нет лока / цель без урона >2.5 с → Escape → F1 → двойной F2 |
| L1 | как ход 81: F2 без урона >3 с и `flow_magnitude<0.01` шесть тиков; peel без `w` |
| ROAM | разворот после 3 пустых; лимит 15 пустых **с последнего килла** |
| Стоп | `kills==10` или `self_hp<0.10`. Таймаута нет. F4 нет |

CLI: `l2-brain s4-integrated-spot --kills 10 --no-timeout --no-heal`.
Выход: `docs/evidence/live-s4/spot-series-10-stable.json`.

## Проверки

`pytest tests/test_spot_loop.py tests/test_s4_probe.py` — 31 зелёный.

## Живой прогон

JSON: [spot-series-10-stable.json](live-s4/spot-series-10-stable.json)
SHA1 `388921ec…`. Окно 16372, PID 68342. Dual flags. **150.0 с**.

`ok=true`, abort нет, **10/10**. `timeout_s=null`. `farm=false`.
`stuck_keys=0`. Старт self HP 0.875. F4 нет. F1 42, F2 22, F3 30.

| kill | поиск | бой | лут | roam |
|---|---|---|---|---|
| 1 | 0.17 с | 2.62 с | 1.74 с | 0 |
| 2 | 20.9 с | 0.11 с* | 1.72 с | 3 |
| 3 | 0.78 с | 0.12 с* | 1.81 с | 0 |
| 4 | 0.23 с | 2.59 с | 1.82 с | 0 |
| 5 | 5.85 с | 5.00 с | 1.82 с | 1 |
| 6 | 11.4 с | 2.99 с | 1.84 с | 2 |
| 7 | 19.9 с | 3.67 с | 1.73 с | 3 |
| 8 | 12.6 с | 4.47 с | 1.83 с | 2 |
| 9 | 5.92 с | 6.53 с | 1.71 с | 1 |
| 10 | 11.5 с | 5.39 с | 1.71 с | 2 |

\* бой 2 и 3: `combat_ms` после сброса таймера на агро, не вся стена
сближения. После агро цель сразу умерла / исчезла.

Средний бой по полю **3.35 с**. `l1_triggers=0`.

`aggro_triggers=2`: Escape→F1 на циклах 2 (после F2, ~30 с) и 3
(во время SCAN). Оба дали `aggro_lock`.

`u_turns=2`: цикл 2 (~23 с) и цикл 7 (~88 с), после 3 пустых ROAM.

encode p95 **53.1 мс**.

F82. ADR-0070.
