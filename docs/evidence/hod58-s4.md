# Ход 58: непрерывный `w`, не пульс

Дата: 2026-09-17. `sim/` и Frozen L1 (52/60) не трогали. Фарм не
открывали. Живого прогона на этом ходе нет.

## Изменение

`live-motion` держал `w` до T1 и отпускал каждый тик — в клиенте
это шажки. Теперь один `down` (`duration_ms=0`), 10+3 сэмпла по
`--hold-ms`, один `up` и `release_all`. Поток — между соседними
кадрами на непрерывном беге.

Пороги ADR-0061 (0.25 и ≥ 3) не меняли.

Тесты: `tests/test_live_motion_probe.py` — 4 зелёных, в том числе
один down / не меньше одного up.

ADR-0062.
