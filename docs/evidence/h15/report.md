# H15 — абляция каналов

Прогон: `tools/h15_ablation.py`, seed=0, `suite=frozen`, 60 эпизодов, `recovery=True`.
Метрики `repair-20260917`. F14/F15 не перезаписывались. Held-out не независим.
Реплик: 1 (как F14). План: [plan.md](plan.md). JSON: [summary.json](summary.json).

## Гипотеза

`Observation.navigation` даёт Δ success ≥ 0.15 против того же контроллера с `color_blob`.

**Отклонена.** `all` 24/60, `blob` 22/60, Δ = +0.033.

## Руки

| рука | успех | Δ к blob | stuck / timeout / oscillate |
|---|---|---|---|
| blob | 22/60 | — | 36 / 2 / 0 |
| target | 22/60 | 0 | 36 / 2 / 0 |
| target_flow | 21/60 | −0.017 | 12 / 18 / 9 |
| target_expansion | 20/60 | −0.033 | 35 / 5 / 0 |
| all | 24/60 | +0.033 | 7 / 18 / 11 |

`target` совпал с blob по исходам. Один поток или одно расширение без остальных рук хуже среднего. Все каналы вместе — плюс два успеха, ниже порога 9.

## Где каналы меняют исход (`blob` → `all`)

| сцена | blob | all |
|---|---|---|
| u_trap / dead_end | 0+0 | 5+5 |
| corridor | 3 | 0 |
| latency_drops | 5 | 0 |
| narrow_gate | 0 (stuck) | 0 (timeout) |
| open_goal / camera_spin | 5 / 5 | 5 / 5 |
| moving_target | 4 | 4 |
| vanish / obstacle / fence / weak | 0 | 0 |

Плюс на ловушках съедается проигрышем коридора (пятно 48×32 ниже 0.12) и сцен с пропусками кадра. Это не прирост политики над F14: другие метрики и другой контроллер.

`all` encode p50 = 3.82 мс после векторизации SAD, не 35 мс.

## Ограничения

Не перенос. Не H16. Не готовность к SNN. Один seed.
