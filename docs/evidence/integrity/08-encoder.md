# Детектор цели и фабрика

Порог `target_min_conf=0.12` и `mass>0.002` не снижали.

Кадр старта, training v0, seed=0. Маски: `target-*.ppm`.

| сцена | пиксели маски | mass | conf | seen | ≥ 0.12 | класс |
|---|---|---|---|---|---|---|
| open_goal | 82 | 0.020 | 0.160 | да | да | видна и детектируется |
| narrow_gate | 64 | 0.016 | 0.125 | да | да | видна и детектируется |
| corridor | 45 | 0.011 | 0.088 | да | нет | видна, детектор ниже порога |
| single_obstacle | 0 | 0 | 0 | нет | нет | цель не видна (LOS) |
| weak_texture | 0 | 0 | 0 | нет | нет | цель не видна |

Ложных красных объектов на этих кадрах нет (маска пуста, когда цели нет в луче).

Фабрика: `l2_brain.factory.build_encoder`. mock/SCK по умолчанию `color_blob` (явное имя в `CircuitConfig.encoder_name`). `baseline-eval` — `navigation_v1`. Различие пишется в extras.
