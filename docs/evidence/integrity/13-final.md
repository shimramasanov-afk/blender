# Итог этапа целостности

SNN, MaleCNS, обучение и Metal не начинали. GT в рабочую политику не подавали.
Исходные `baseline-nav.json` / `baseline-memory-nav.json` не перезаписывались.

## 1. Аудит: что подтвердилось

- 23/60 у baseline и memory, по эпизодам поля поведения совпадают.
- Восстановление на каталоге не включалось; GT-окон неподвижности 0.
- `u_trap`/`dead_end`: LOS есть, цели нет в FOV, поворот на месте достаточен.
- Скрытая/слабая цель: obstacle/fence/weak — 0 пикселей цели; corridor — 45 px, conf 0.088 < 0.12.
- `narrow_gate`: цель видна (64 px, conf 0.125), база билась о створки.
- Vanish и дропы ломают курс.
- Два `stuck` (исход эпизода ≠ physical_blockage).
- Две линейки сцен (S0 `eval` vs nav-sim).
- mock/SCK = `color_blob`, оценка = `navigation_v1`.
- Польза потока в контуре проверена: H15 отклонена (24 vs 22, Δ +0.033).

## 2. Что в аудите было неточно или неполно

- «Простой поворот к цели» на ловушках: в JSON нет лога намерений; подтверждено геометрией (LOS + поворот на месте), не трейсом политики.
- Recall в артефакте memory уже был `null`, не 0.
- `GroundTruth.goal_visible` — флаг «не спрятали», не пиксель/LOS.
- `corridor` геометрически `visible_goal`; провал — порог детектора, не отсутствие цели в мире.
- После правок контроллера полный 23/60 заново не снимали — смешение со стендом запрещено.

## 3. Что исправлено

| блок | результат |
|---|---|
| 1 | freeze + воспроизведение 23/60, 0 расхождений |
| 2 | одна приёмка: nav-sim-v1; S0 — дым; H15 открыта; H16 не подтверждена |
| 3 | пересекающиеся события, пороги в секундах, recall = N/A без окон |
| 4 | таблица наблюдаемости |
| 5 | имена `rear_goal_*`; новые сцены; `push_wall` даёт GT-блокировку; заднего хода нет |
| 6 | единицы и a priori бюджет; frozen 200 сохранён |
| 7 | A/B/C: ворота=управление, коридор=зрение, vanish=память, obstacle=исследование |
| 8 | фабрика энкодера; маски; порог не снижали |
| 9–10 | курс и избегание: локальные замеры, не полный каталог |
| 11 | H15 отклонена: blob 22, all 24; поток/расширение по отдельности хуже |

## 4. Сопоставимое до/после

Официальное сравнение контроллеров на frozen seed=0: **без изменений 23/60** (воспроизведение).

Локальные пробы training v0 (не каталог):

| сцена | F14 v0 | после 9–10 |
|---|---|---|
| open_goal | успех 173 | успех 170 |
| camera_spin | успех 98 | успех 96 |
| narrow_gate | stuck, 33 столкновения | timeout, 0 столкновений, net 2.33 |
| vanishing_target | stuck, net 0.54 | timeout, net 0.63 |

A/B/C (привилегия, не визуальный зачёт): ворота и коридор проходимы при эталонном пеленге; vanish и obstacle — нет.

## 5. Нерешённые ограничения

- Исследование за стеной без подсказки стороны.
- Курс после hide всё ещё короче пути до цели.
- Ворота: без ударов, без финиша в 200 тиках.
- Коридор: пятно есть, порог 0.12 не берёт; порог не снижали.
- `side_hold_jog` с места цель не видит.
- H15 отклонена. Held-out F14 больше не независим.
- Новый отложенный held-out не собран.

## 6. Готовность к recurrent / SNN

**Не готов.** Постановка измерима, H15/H16 оценены. Контур всё ещё не решает ворота, hide, исследование и коридор. Сравнивать recurrent/SNN — сравнивать на той же дырявой политике.

Снимок после ремонта: 24/60 в `h15/all.json`, не замена F14. MaleCNS и Metal не начинать.
