# Ход 60: линейка live-motion закрыта

Дата: 2026-09-17. `sim/` и Frozen L1 (52/60) не трогали. Фарм не
открывали. Нового прогона нет. Клавиши сняты.

## Итог линейки (F61–F65)

| факт | сцена | ввод | flow free/blocked | пик | stuck |
|---|---|---|---|---|---|
| F61 | поляна | пульс `w` | 0 / 0 | 0 | false |
| F62 | ворота | пульс `w` | 0.009 / 0 | 0.045 | false |
| F63 | стена | пульс `w` | 0.014 / 0 | 0.050 | false |
| F64 | подход | пульс `w` | 0.014 / 0 | **0.095** | false |
| F65 | подход | непрерывный `w` | 0.005 / 0 | 0.048 | false |

Доказано: HID доходит; T0≠T1; у стены кадр может замереть
(`duplicate`, confidence 0). Не доказано: `flow_free ≥ 0.25`,
контраст ≥ 3, yaw (Q5), готовность Frozen L1.

Протокол ввода: ADR-0062 (один `w`). Пороги: ADR-0061.

## Стоп

Клиент завис. Окно **13944** и PID **68342** после рестарта не
использовать. HID не слать, пока нет нового `capture list`.

## Старт после рестарта

1. Клиент Interlude в видимом окне Parallels (не скрытый стол).
2. `.venv/bin/python -m l2_brain capture list` — новый `id`.
3. PID `com.parallels.desktop.console`.
4. Свободная прямая **~7 с**, затем упор **~2 с**.
5. `live-motion --window-id <новый> --live --danger-confirmed --target-pid <новый> --out docs/evidence/live-s4/l1-flow-validation-<имя>.json`

ADR-0063.
