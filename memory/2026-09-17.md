# 2026-09-17

## Ход 34

H7 принята на видимом окне Parallels `Windows 11` id 13745.
30.13 с: медиана 33.76 мс, 29.62 FPS, drops 0, black 0,
latency p50/p95 10.97/12.73 мс. SHA1
`8a8c6c24e99fd0e616906f6453859a069a1f5355`. ADR-0041, F40.
HID нет. Этап 2 (HUD) не открыт.

## Ход 35

Синтетический HUD-парсер и мост `ui_vision`. 9 тестов зелёные.
ROI в `parallels_l2.json` — типичный Classic, не замер клиента.
GT стенда не снимали. ADR-0042, F41.

## Ход 36

CGEvent backend с двумя флагами. Live-probe aborted: `l2.exe` в госте,
hid_sent=false. Watchdog на RecordingPoster stuck=0. Блокнот не
подтверждён. ADR-0043, F42.

## Ход 37

Live-probe в Notepad: нет `L2.exe`, hid_sent=true, stuck=0, watchdog
отпустил w. p50/p95 215.6/244.1 мс. Кадр: 123 в открытом l2.log
(это Notepad, не клиент). F43. S4 не открыт.

## Ход 38

NSWorkspace вместо osascript: focus p50 0.0047 мс, контроль osascript
175.9 мс. act_ms RecordingPoster 0.016 мс, hid_sent=false.
Сняли видимое окно 13944: Interlude оконный, плейсхолдеры HUD мимо
полос, статус-виджет не выделен. Хроника Interlude по чату. ADR-0044,
F44. S4 не открыт. Боевой ввод не слали.

## Ход 46

`--multi-kill 3` на окне 13944: 3 фрага, 3 F3, 27.33 с, stuck=0,
watchdog=false, farm=false. ADR-0053, F53. Не бесконечный фарм.

## Ход 47

`--verify-search`: F1×2 miss, CameraRotate 150, w 400 мс, F1,
search_no_target. stuck=0, watchdog=false. Килла нет. ADR-0054, F54.

## Ход 48

Камера `right_arrow` 450 мс, без мыши. F1 сразу 0.444, стрелка
ушла, килл 0.444→0.0, F3, stuck=0. Yaw глазами не видели.
ADR-0055, F55.

## Ход 49

calibrate-motion: yaw 20.8 px peak 0.023, step 165 px peak 0.034,
deg/s 0.76 при FOV 75° (допуск). H8 и Q5 не закрыты. ADR-0056, F56.

## Ход 50

bench-h8 n=100 окно 13944: p95 H8 2.19 мс, pipeline 16.33 мс,
encode p95 44.26 мс, hid_sent=false. Горизонт peak 0.013 < 0.15,
профиль не писали. H8 принята. Q5 нет. ADR-0057, F57.

## Ход 51

KLT yaw OpenCV: inliers 22/25 < 30, median_dx ≈ 0, Q5 не закрыт,
профиль не писали. ADR-0058, F58.

## Ход 52

RMB Drag +150 px и 10 тапов стрелки. KLT сошёлся (39/39 инлаеров,
ratio 0.83/0.78), median_dx ≈ 0. Q5 не закрыт, профиль не писали.
ADR-0059, F59.

## Ход 53

sync-diag: T0/T1 не один буфер, diff_mean 7.05, Δt 371 мс,
KLT dx ≈ 0. SCK desync не подтверждён. Q5 нет. ADR-0060, F60.

## Ход 54

live-motion 10+3 × w 600 мс: flow_magnitude 0, expansion 0,
motion_confidence ≈ 0.40, encode p95 48 мс, stuck_detected=false.
Frozen L1 не трогали. ADR-0061, F61.

## Ход 55

live-motion у ворот: flow 0.0089/0, confidence ≈ 0.51/0.53,
encode p95 50.1 мс, stuck_detected=false. F62.

## Ход 56

live-motion у стены: flow 0.0137/0, confidence ≈ 0.45/0.16.
blocked[1..2] duplicate + confidence 0. encode p95 48.4 мс,
stuck_detected=false. F63.

## Ход 57

live-motion подход: flow 0.0139/0, пик free 0.095.
С free[5] duplicate + confidence 0. encode p95 47.7 мс,
stuck_detected=false. F64.

## Ход 58

live-motion: непрерывный w, hold-ms = период сэмпла. ADR-0062.
Живого прогона нет.

## Ход 59

непрерывный w в клиенте: 2 HoldKey, stuck_keys=0, flow 0.0048/0,
пик 0.048, с free[6] duplicate. encode p95 48.4 мс,
stuck_detected=false. F65.

## Ход 60

линейка live-motion закрыта. Q5 открыт, L1 stuck нет. Окно 13944
и PID 68342 списать. ADR-0063. После рестарта — новый capture list.
