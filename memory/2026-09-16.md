# 2026-09-16

## Старт L2_brain

- Каталог был пуст. Операционных инструкций нет — человек пишет их сам.
- Положена документация v1 и синтетический стенд с тремя контроллерами.
- Живой клиент, SCK в петле и HID закрыты заглушками.
- MaleCNS не зависимость.
- Следующий неблокированный этап — S1 (цифры на стенде). S2–S4 ждут замеров и явной команды.
- S0 прогнан: 17 тестов OK. Дым `open_field` ×8: все три контроллера success=1.00; `infer_ms` ~0.04, `loop_ms` ~0.95. Это не приёмка H1.
- CaptureProbe собрался и перечислил 21 видимое окно. Интервалы H7 не снимали.

## Правила и skills

- Добавлены `.cursor/rules/` (только 00 alwaysApply) и 7 project skills.
- Новые доки: project-brief, open-questions, status, agent-workflows.
- Существующие vision/architecture/assumptions/roadmap/decisions не перезаписывались.
- Реализация агента в этом ходе не велась.

## Отчёт

- Канон хронологии: `docs/work-report.md` (ходы 1–4). Ссылка из `docs/status.md`.

## Каркас контура

- `l2_brain.circuit` + CLI mock/record/replay/eval/doctor. 28 тестов OK.

## Навигационный стенд

- `l2_brain.sim`, 12 сцен, MotorIntent, GT отдельно. 38 тестов OK.
- `sim-eval` пишет JSON. Успех стенда ≠ перенос в MMORPG.
- Mock 16 тиков записан в `run/mock-session.jsonl`. Клиента нет. S1 не закрыт.

## Эксперименты

- `l2_brain.experiment`: mono/event часы, latest-only, async writer, inspect тика.
- 50 тестов OK. Offline replay ≠ closed-loop.

## ScreenCaptureKit

- `SCKFrameSource` + pipe `L2F1`. 60 тестов OK.
- Self-test окно записано и replay. H7 не закрыта. Permission не запрашиваем.

## VisualEncoder

- `NavigationEncoder` 48×32: SAD-поток + поиск масштаба. Не YOLO.
- Bench: синтетика 6/6, sim отделяет поворот/подход; 32×24 на стенде не разделяет. encode p50 ≈ 36.6 мс.
- 73 теста OK. Контур политики не гоняли. H15 открыта.

## Базовый контроллер

- `BaselineController` без обучения. Каталог seed=0: 23/60. vision 20, control 17.
- База для H15. Маршруты под карты не клали. 84 теста OK.

## Целостность экспериментов

- F14/F15 воспроизведены, исходные JSON не трогали. Канон — nav-sim frozen.
- Два stuck разделены. `goal_visible` ≠ LOS. corridor: 45 px, conf 0.088.
- A/B/C: gate=control, corridor=vision, vanish=memory, obstacle=exploration.
- H15 не гоняли. К recurrent/SNN не готов.

## Память и восстановление

- `baseline_memory_v1`: пеленг, сторона, progress_conf, evidence, попытки. Без карты.
- Каталог seed=0 совпал с F14. Ловушки не улучшились (уже 10/10). GT-stuck окон 0.
- H16 не подтверждена. Не биология. 93 теста OK.
